<Grid Height="110" Width="480" Margin="10" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid.ColumnDefinitions>
    <ColumnDefinition Width="Auto"/>
    <ColumnDefinition Width="*"/>
  </Grid.ColumnDefinitions>
  <Border Background="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="110" Height="110">
    <Image x:Name="Replace_Windows.UI.Xaml.Media.ImageSource" Source="{Binding}" Height="110" Width="110"/>
  </Border>
  <StackPanel Grid.Column="1" VertcalAlignment="Top" Margin="10,0,0,0">
    <TextBlock x:Name="Replace_Title_System.String" Text="{Binding}" Style="{StaticResource TitleTextStyle}"/>
    <TextBlock x:Name="Replace_Subtitle_System.String" Text="{Binding}" Style="{StaticResource CaptionTextStyle}" TextWrapping="NoWrap"/>
  </StackPanel>
</Grid>